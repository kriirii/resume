<div class="p-3">
    <hr>
    <div class="my-4">
        <footer class="footer">
            <div class="container">
                <div class="row">
                    <div class="nav">
                        {{#each footer}}
                        <div class="nav-col2 col-2">
                            <div>
                                {{#each this}}
                                <div>
                                    <a class="nav-link{{#if @last}} disabled{{/if}}" href="{{link}}">{{text}}</a>
                                </div>
                                {{/each}}
                            </div>
                        </div>
                        {{/each}}
                    </div>
                </div>
            </div>
        </footer>
    </div>
</div>